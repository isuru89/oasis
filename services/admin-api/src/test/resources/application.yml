spring:
  profiles:
    active: test

  devtools:
    restart: true

  datasource:
    type: com.zaxxer.hikari.HikariDataSource
    driverClassName: org.sqlite.JDBC
    jdbcUrl: jdbc:sqlite:memory:myDb
    username: root
    password: root
    hikari:
      poolName: Hikari
      auto-commit: false


